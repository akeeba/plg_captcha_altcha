/.idea/laravel-idea.xml
/node_modules/**
/vendor/
/release/**
/security.md

### Release credentials
# release.yaml / release.json are written here with all placeholders substituted
# at release time and deleted afterwards. An aborted release leaves them behind.
/release.yaml
/release.json
